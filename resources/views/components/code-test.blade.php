{{-- @props('languages' => [])

<div class="mx-auto max-w-4xl p-8" x-data="{ tab: 'laravel' }">
        <div class="flex gap-2 border-b mb-4 bg-red-500">
            @foreach ($languages as $language)
               <button @click="tab = '{{ $language }}'" class="px-3 py-2 border rounded">
                  {{ $language }}
               </button>
            @endforeach
        </div>

        <div x-show="tab === 'laravel'">
            Basic Laravel
        </div>
    </div> --}}
